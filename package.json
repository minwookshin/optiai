{
  "name": "optiai",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "for file in scripts/*.mjs scripts/lib/*.mjs tests/*.mjs; do node --check \"$file\" || exit 1; done",
    "test": "node --test",
    "preference-lab": "node scripts/create-preference-lab.mjs",
    "export-preferences": "node scripts/export-preferences.mjs",
    "aggregate-preferences": "node scripts/aggregate-preferences.mjs",
    "train-preference-ranker": "node scripts/train-preference-ranker.mjs",
    "adaptive-preference-lab": "node scripts/create-adaptive-preference-lab.mjs",
    "export-adaptive-preferences": "node scripts/export-adaptive-preferences.mjs",
    "estimate-ideal-point": "node scripts/estimate-ideal-point.mjs",
    "create-fixed-benchmark": "node scripts/create-fixed-benchmark.mjs",
    "evaluate-fixed-benchmark": "node scripts/evaluate-fixed-benchmark.mjs",
    "check-benchmark-promotion": "node scripts/check-benchmark-promotion.mjs"
  },
  "dependencies": {
    "@resvg/resvg-js": "2.6.2",
    "@xmldom/xmldom": "0.9.10"
  }
}
