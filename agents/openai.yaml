interface:
  display_name: "OptiAI"
  short_description: "Audit, label, and calibrate SVG optical alignment"
  default_prompt: "Use $optiai to audit this SVG, collect response-driven blinded panel evidence when useful, estimate tie-aware calibration, and apply only a verified correction."
