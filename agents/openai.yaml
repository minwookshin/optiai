interface:
  display_name: "OptiAI"
  short_description: "Measure and safely correct SVG optical alignment"
  default_prompt: "Use $optiai to audit this SVG, review its optical offset, and apply only a clipping-safe verified correction."
